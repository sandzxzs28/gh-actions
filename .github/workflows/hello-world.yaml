on:
  push:
    branches:
      - main  # Or whichever branch you want

jobs:
  hello:
    name: Say Hello Sandzxzs. This is GH runner.
    runs-on: self-hosted

    steps:
      - name: Print Hello World
        run: echo "Hello, World from self-hosted runner! This is GH runner!"
